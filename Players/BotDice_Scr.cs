using UnityEngine;

public class BotDice_Scr : MonoBehaviour
{
    public BotPlayer_Scr player;
    public int id;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
